[package]
name = "spine"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "Canonical governance crate for the control-spine family: typed findings, signoff receipts, evidence packs, fail-closed verification."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
